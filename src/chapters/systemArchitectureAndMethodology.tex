\chapter{System Architecture and Methodology}
\label{chap:system_architecture_methodology}

% Overview of the DQN-based system design integrating SUMO simulation with reinforcement learning
\section{System Architecture}
\label{sec:system_architecture}

The system employs a Deep Q-Network architecture for adaptive traffic signal control at the Thapathali T1 intersection, integrating the SUMO microscopic traffic simulation platform as the environment with a custom reinforcement learning framework designed specifically for the intersection's geometry and traffic demand characteristics. The overall architecture follows the standard reinforcement learning loop where the agent observes the current state of the intersection, selects a signal control action, receives a reward signal reflecting the quality of that decision, and updates its policy through experience-driven learning.

The state representation module processes real-time traffic data collected at each 15-second decision step through SUMO's TraCI interface. PCU-weighted vehicle loads are computed for the 7 origin lanes approaching the intersection and the 5 outgoing lanes discharging traffic away from it, capturing the instantaneous flow conditions across all major approaches. Maximum waiting times are extracted for each of the 6 stopline lanes, identifying the most severely delayed vehicles queuing at the signal. The currently active signal phase is encoded as a 3-dimensional one-hot vector to inform the agent of its current operating context. Together these components are concatenated into a fixed-length 21-dimensional feature vector that comprehensively represents the intersection status at each decision point.

This state vector feeds into dual neural networks that form the core of the DQN learning mechanism. The Q-network receives the 21-dimensional state vector as input and passes it through three fully connected hidden layers of 128, 64, and 32 neurons respectively using ReLU activation functions, designed to capture complex non-linear relationships between traffic states and optimal signal timing decisions. The output layer produces 2 Q-values, one for each possible action, representing the agent's estimated cumulative future reward for selecting each action from the current state. A separate target network maintains an identical architecture but updates its weights less frequently by periodically copying from the Q-network, providing stable learning targets that prevent oscillations and divergence during training.

A replay buffer stores up to 10,000 past experience tuples, each consisting of the observed state, selected action, received reward, resulting next state, and episode termination flag. During each training step, a random mini-batch of 64 transitions is sampled from this buffer to compute the Q-learning update, breaking the temporal correlations between consecutive experiences and preventing the network from overfitting to recent transitions. This experience replay mechanism significantly improves the stability and sample efficiency of the learning process across the full training run of 300 episodes.

An $\epsilon$-greedy exploration strategy governs action selection throughout training. In early episodes the agent selects actions randomly with high probability, broadly exploring the state space and populating the replay buffer with diverse experiences. As training progresses $\epsilon$ decays multiplicatively after each episode, gradually shifting the agent towards exploiting its learned Q-values for action selection. By the final episodes the agent operates almost entirely on its learned policy, with only a small residual probability of random exploration retained to avoid becoming trapped in suboptimal behaviours.

The action space consists of 2 discrete actions - STAY and NEXT - applied over a cyclic sequence of 3 predefined signal phases. At each 15-second decision interval the agent either maintains the current active phase to continue serving the same traffic direction or advances to the next phase in the cycle to serve a different approach. Each phase transition includes a 3-second yellow interval to ensure safe and real

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{src/images/figures/system_diagram.jpeg} 
    \caption{Block Diagram of DQN-Based Traffic Signal Control System}
    \label{fig:system_block_diagram}
\end{figure}


\section{Use Case Diagram}
\label{sec:use_case_diagram}

Figure \ref{fig:use_case_diagram} presents the use case diagram for the Thapathali DQN Traffic
Signal Control System. The system involves four actors: the Traffic
Engineer, Evaluation Analyst, SUMO Simulator, and DQN Agent. The Traffic
Engineer initiates the system by configuring the simulation, loading the
trained model, and starting the simulation. The Evaluation Analyst is
responsible for generating performance metrics and triggering the
comparison between the DQN agent and the fixed timer baseline. The SUMO
Simulator acts as the external simulation environment supporting both
training and evaluation. The DQN Agent autonomously observes traffic
state, selects signal actions, stores experiences, applies signal
phases, and calculates rewards during training. The Train DQN Model use
case includes five mandatory sub-processes: observe traffic state,
select signal action, apply signal phase, store experience, and
calculate reward, all executed at every decision step. Update Standalone
Signals extends the training process under specific traffic conditions,
while Compare DQN vs Fixed Timer extends from Generate Performance
Metrics and triggers the Run Test Scenario use case for evaluation.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{src/images/figures/USECASE_REPORT.png}
    \caption{Use case diagram of the Thapathali DQN traffic signal control system}
    \label{fig:use_case_diagram}
\end{figure}

\section{Class Diagram}
\label{sec:class_diagram}

Figure \ref{fig:class_diagram} presents the class diagram illustrating the structural
relationships between the five core classes of the system. The TrainDQN
class serves as the entry point, creating the SumoEnvironment and using
the DQNAgent to manage the training loop. The SumoEnvironment class
encapsulates all simulation interaction through SUMO\textquotesingle s
TraCI interface, maintaining lane-level traffic data and exposing
methods for state observation, reward calculation, phase control, and
metric collection. The DQNAgent class manages the reinforcement learning
logic, holding references to two DQNetwork instances -- the Q-network
for action selection and the target network for stable learning targets
-- and a ReplayBuffer for experience storage. The DQNetwork class
implements the neural network with three fully connected hidden layers.
The config class acts as a shared configuration module, read by both
SumoEnvironment and DQNAgent to maintain consistent hyperparameters and
simulation settings throughout the system. The FixedTimerController
class operates independently and compares against SumoEnvironment during
evaluation.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{src/images/figures/Class_Diagram.png}
    \caption{Class diagram of the Thapathali DQN traffic signal control system}
    \label{fig:class_diagram}
\end{figure}

\section{Sequence Diagram}
\label{sec:sequence_diagram}

Figure \ref{fig:sequence_diagram} presents the sequence diagram illustrating the end-to-end
flow of the system from initialisation through training to final
evaluation. The traffic engineer triggers the process by calling
train(), which initialises the DQNAgent with its neural networks and
replay buffer and the SumoEnvironment, which loads the network and flow
files into SUMO. During the 300-episode training loop, each episode
begins with a reset and proceeds through repeated decision steps where
the agent observes the state, selects an action, and applies the signal
phase for 15 seconds of green time. When the agent selects NEXT, a
3-second yellow transition is executed before switching to the next
phase, ensuring safe and realistic signal operation. The agent then
receives the reward, stores the experience, and performs a training
step. After each episode epsilon decays, the target network synchronises
every 10 episodes, and checkpoints are saved every 5 episodes. Upon
completion the final model is saved and training plots are generated.
During evaluation the Evaluation Analyst runs the FixedTimerController
on the same test scenario to collect baseline metrics, then runs the DQN
agent in test mode using the saved model to collect DQN metrics, and
finally compares both results to generate the performance report.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{src/images/figures/Sequence_diagram.png}
    \caption{Sequence diagram of the Thapathali DQN traffic signal control system}
    \label{fig:sequence_diagram}
\end{figure}

% Data sources and field survey methodology used to ground the simulation in real traffic patterns
\section{Data Analysis}
\label{sec:data_analysis}

\subsection{Data Collection}
\label{subsec:data_collection}
For the traffic network modeling, primary traffic flow data was sourced from the research paper \textit{A Case Study of Thapathali Intersection} by Lav Maharjan \cite{maharjan2023performance}. The data collection methodology involved comprehensive field surveys over a duration of five days, strategically selected to capture both weekday (3 days) and weekend (2 days) traffic characteristics. The observations were conducted continuously from 6:00 AM to 10:15 PM, ensuring the inclusion of morning and evening peak periods as well as off-peak transitions.

The data collected during the surveys were represented in terms of \gls{pcu} \gls{od} data and vehicle counts for different time intervals. \gls{pcu} \gls{od} data was recorded for two specific time intervals: one during the morning peak period and one during the evening peak period, each lasting one hour. These intervals were selected to capture the highest traffic flow during the day. The vehicle counts were recorded separately for these intervals within the given time frame. The representation of the data allows for a clearer analysis of traffic behavior during peak and off-peak times, providing a foundation for modeling the traffic flow more accurately.

\subsection{Data Representation}
\label{subsec:data_representation}
\begin{table}[H]
    \centering
    \small
    \caption{Vehicle Class and Adopted PCU Factors}
    \label{tab:pcu_factors}
    \begin{tabular}{p{7cm}p{3cm}c}
        \toprule
        \textbf{Vehicle Class} & \textbf{Representation} & \textbf{Adopted PCU Factor} \\
        \midrule
        Light Vehicle & LV & 1.5 \\
        Heavy Vehicle & HV & 3 \\
        Bus & B & 3 \\
        Mini Bus & MB & 2.5 \\
        Micro Bus & MIB & 1.5 \\
        Car & C & 1 \\
        Four-Wheel & F & 1 \\
        Tempo & T & 0.75 \\
        Utility & U & 0.75 \\
        Two-Wheeler & TW & 0.25 \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{OD Data Allocation}
\label{subsec:od_allocation}
The vehicle allocation system is implemented using a Python script, leveraging libraries like \texttt{pandas}, \texttt{numpy}, and \texttt{matplotlib} for data manipulation and analysis. The following summarizes the key components of the implementation.

The primary input data consists of:
\begin{itemize}
    \item \textbf{Vehicle Count Data (\texttt{vehicle\_count.xlsx}):} Contains the number of vehicles for various vehicle classes during 15-minute intervals.
    \item \textbf{PCU OD Data (\texttt{pcu\_od.xlsx}):} Provides the proportions of \gls{pcu} allocated to each \gls{od} pair for AM and PM periods.
\end{itemize}

\subsubsection{Data Cleaning}
\begin{itemize}
    \item Non-numeric characters are removed from vehicle count and \gls{pcu} data.
    \item Invalid data is replaced with zero.
    \item Data is converted to numeric format for proper calculation and allocation.
    \item Vehicles with low counts were grouped into similar vehicle types.
    \item Vehicles with same \gls{pcu} and sizes were grouped into one vehicle type.
\end{itemize}

\subsubsection{Vehicle Allocation Process}
For each vehicle class and \gls{od} pair, the following process is followed:
\begin{itemize}
    \item \textbf{Proportions Calculation:} AM and PM proportions are calculated based on the \gls{pcu} data.
    \item \textbf{Allocation via Largest Remainder Method:} Vehicles are initially allocated proportionally, and leftover vehicles are distributed to ODs with the largest fractional values.
    \item \textbf{Piecewise Interpolation:} For each 15-minute interval, vehicle proportions are dynamically interpolated using piecewise interpolation.
\end{itemize}

\subsubsection{Maternity Restrictions}
For certain ODs related to maternity, only specific vehicle classes are allowed (\eg cars, two-wheelers, four-wheel vehicles, and utility vehicles), based on real-world traffic constraints.

\subsubsection{Output Generation}
The final allocated vehicle counts are saved to Excel files, including:
\begin{itemize}
    \item 15-minute interval counts for every \gls{od} and vehicle class.
    \item A summary sheet showing total vehicles and AM/PM proportions.
    \item A verification sheet ensuring correct and consistent allocation.
\end{itemize}

\section{Environment Setup for Traffic Simulation}
\label{sec:env_setup}

The first step in the environment setup for traffic simulation using \gls{sumo} was to use the NETEDIT tool to create a road network that accurately represents the Thapathali intersection. This tool allows for the design of road networks, including multiple lanes, junctions, and traffic signals. The geometry of the intersection and road structure was designed in NETEDIT and saved as the \texttt{map.net.xml} file. This file contains the road network's topography, including nodes (junctions), edges (roads), and their connections. It also defines traffic signals at each intersection, which are crucial for simulating realistic traffic flow.

Once the network structure was defined, vehicle movement was modeled using the \texttt{routes.rou.xml} file. This file specifies the routes that vehicles will take through the network and includes information such as departure times, vehicle types, and flow. The vehicle flow is generated to ensure that vehicles enter the network at regular intervals, with a preference for the least congested lane, thus simulating realistic driving behavior.

The \texttt{vtypes.xml} file defines various vehicle types used in the simulation, each with specific attributes such as length, maximum speed, acceleration, and deceleration. These characteristics are essential for accurately simulating vehicle behavior across different classes.

The \texttt{flows.xml} file captures vehicle flow data and traffic signal phases. This file defines how many vehicles pass through each edge of the network in specific time intervals and the corresponding traffic signal phases.

To interact with the simulation in real-time, the \gls{traci} interface is used. This allows external scripts to control the simulation, modifying vehicle flows and traffic signal phases dynamically. A Python script adjusts the signal phases based on real-time data, such as vehicle counts and congestion levels.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{src/images/figures/sumoenviroment.jpeg}
    \caption{SUMO Environment Block Diagram}
    \label{fig:sumo_env_diagram}
\end{figure}

% Design of the DQN agent including state vector, action space, reward shaping, and network layers
\section{DQN Agent Development}
\label{sec:dqn_development}

\subsection{State Representation}
The \gls{dqn} agent observed key traffic conditions at each 15-second decision step, including PCU-weighted vehicle loads on incoming and outgoing lanes, maximum waiting times at stopline lanes, and the current active signal phase. These parameters were combined into a fixed-length 21-dimensional state vector representing the intersection status at each decision step.


\[
s_t = \bigl[ 
  \underbrace{p_1, p_2, \dots, p_7}_{\text{Incoming PCU (7)}}, \;
  \underbrace{w_1, w_2, \dots, w_6}_{\text{Stopline Max Wait (6)}}, \;
  \underbrace{p_{o1}, p_{o2}, \dots, p_{o5}}_{\text{Outgoing PCU (5)}}, \;
  \underbrace{0, 1, 0}_{\text{Current Phase (3)}}
\bigr]
\]


\subsection{Action Space}

The traffic signal at Thapathali T1 operates across three predefined phases (P1, P2, P3) in a cyclic sequence. At every 15-second decision interval the DQN agent selects one of two actions - Action 1 (STAY) or Action 2 (NEXT) - to control the signal timing. A phase can be held for a maximum of 60 seconds (4 consecutive STAY actions) before automatically advancing to the next phase, ensuring no traffic direction is starved indefinitely.
\begin{itemize}
  \item \textbf{Action 1 (STAY)}: 
The agent maintains the current active phase for another 15 seconds, allowing continued service to the same direction.
  \item \textbf{Action 2 (NEXT)}: The agent advances to the next phase in the cycle, switching service to the next direction. Each phase transition includes a 3-second yellow interval to ensure realistic and safe signal timing.
\end{itemize}

\subsection{Reward Function}
\noindent A reward function based on Max-Pressure Theory was used. Pressure was calculated as the difference between incoming and outgoing vehicle PCU counts:

\begin{equation}
R = -\left(\sum_{l \in L_{\text{stop}}} \text{PCU}(l) - \sum_{l \in L_{\text{out}}} \text{PCU}(l)\right) - \lambda \sum_{l \in L_{\text{stop}}} \sum_{v \in l} \text{PCU}(v) \cdot w^{acc}(v)
\end{equation}

\textbf{Where:}
\begin{itemize}
    \item $L_{\text{stop}}$ — set of 6 stopline lanes
    \item $L_{\text{out}}$ — set of 5 outgoing lanes
    \item $\mathrm{PCU}(l)$ — total PCU-weighted vehicle load on lane $l$ at current step
    \item $\mathrm{PCU}(v)$ — PCU value of individual vehicle $v$
    \item $w^{\text{acc}}(v)$ — accumulated waiting time (seconds) of vehicle $v$ since simulation start
    \item $\lambda = 0.0004$ — weighting factor balancing throughput and wait time penalty
\end{itemize}


\section{Design Justification}
\label{sec:design_justification}

Several reward formulations were considered before adopting the
Max-Pressure based reward. A pure delay-based reward penalising total
accumulated waiting time across all vehicles was ruled out because
accumulated waiting time grows unboundedly within an episode, causing
Q-value targets to grow without bound and leading to loss divergence
during training. A throughput-only reward rewarding the agent purely for
vehicles cleared was also considered but rejected because outgoing lane
flow at Thapathali is heavily influenced by road width and geometry
rather than signal decisions alone, meaning the agent would learn to
favour wider outgoing lanes regardless of which approach actually had
the heaviest queue.

The adopted formulation is grounded in Max-Pressure theory, which has
theoretical throughput optimality properties under certain demand
conditions and has been validated in prior DRL traffic signal control
literature. The first term computes the pressure difference between
stopline PCU accumulation and outgoing PCU discharge - directly
measuring whether the agent\textquotesingle s current phase choice is
clearing vehicles efficiently. The second term adds a PCU-weighted
accumulated waiting time penalty scaled by $\lambda =
0.0004$, discouraging the agent from ignoring any single approach for
extended periods. The $\lambda$ value was calibrated to
keep the wait penalty secondary to the throughput objective, preventing
the agent from sacrificing overall flow in favour of minimising wait
time on a single approach.

\subsection{Impact on Learning}
\label{subsec:impact_on_learning}

The reward function directly shapes the Q-value targets the agent learns
from at every training step. The pressure term
$-(\sum_{l \in L_{stop}} \text{PCU}(l) - \sum_{l \in L_{out}} \text{PCU}(l))$
provides an instantaneous signal that reflects the current state of the
intersection - when the agent selects the correct phase, outgoing PCU
increases and stopline PCU decreases, producing a less negative reward
that reinforces the action. When the agent holds the wrong phase,
stopline PCU accumulates and outgoing PCU stays low, producing a more
negative reward that discourages the action. This direct
cause-and-effect relationship between phase selection and reward is
essential for stable Q-value learning.

The accumulated wait penalty $\lambda \sum_{v} \text{PCU}(v) \cdot w^{acc}(v)$
complements the pressure term by introducing a fairness objective-the
agent is penalised for vehicles that have been waiting for extended
periods regardless of which lane they occupy. This prevents the agent
from learning a degenerate policy that maximises throughput on one
high-flow approach while consistently starving lower-volume approaches
of green time.

Together the two terms ensure that the agent learns to balance both
efficiency -clearing the maximum number of PCU per unit time - and
equity - preventing excessive delay accumulation on any single approach.
This dual objective aligns directly with the project\textquotesingle s
performance metrics of average wait time reduction and throughput
efficiency improvement, ensuring that what the agent optimises during
training corresponds to what is measured during evaluation.

\subsection{Neural Network Architecture}

The agent was implemented using a deep neural network with one input layer, three hidden layers of 128, 64, and 32 neurons respectively, and an output layer with two action values corresponding to STAY and NEXT. The input layer accepts the 21-dimensional state vector representing current intersection conditions. ReLU activation functions were applied after each hidden layer to introduce non-linearity. The output layer produces Q-values for each of the two possible actions, from which the agent selects the action with the highest estimated future reward.

Two identical networks were used: a Q-network for action selection and a target network to stabilise learning during training. The target network weights were updated every 10 episodes by copying the Q-network weights, preventing oscillations and divergence during the training process.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{src/images/figures/NN.png}
    \caption{Neural Network Architecture for Q-value Prediction}
    \label{fig:nn_architecture}
\end{figure}

% Training loop covering epsilon-greedy exploration, experience replay, and target network updates
\section{Training Process}
\label{sec:training_process}

\subsection{Learning Strategy}

An $\epsilon$-greedy learning strategy was used to balance exploration and exploitation during training. The agent initially explored the environment by selecting random actions with $\epsilon$ set to 1.0, ensuring broad coverage of the state space in early episodes. After each episode, $\epsilon$ was multiplicatively decayed by a factor of 0.985, gradually reducing random exploration and allowing the agent to increasingly rely on its learned Q-values for action selection. By approximately episode 300, $\epsilon$ reached its minimum value of 0.01, at which point the agent exploits its learned policy for 99\% of decisions while retaining a small probability of random exploration to avoid getting trapped in suboptimal policies.
\subsection{Experience Replay}
\noindent Experience replay improved stability by storing transitions in a 10,000-capacity buffer. Random samples were used for training to break temporal correlations.

\subsection{Training Loop}
\noindent The model was trained over 300 episodes. The process included:
\begin{enumerate}
    \item Calculate Target Q: $Q_{\text{target}} = r + \gamma \times \max(Q(s', a'))$.
    \item Calculate Current Q: $Q_{\text{current}} = Q(s, a)$.
    \item Backpropagate Loss: $\text{MSE}(Q_{\text{target}}, Q_{\text{current}})$.
    \item Update policy network weights.
\end{enumerate}

% Pseudocode for the max-pressure reward calculation and the DQN training procedure
\section{Algorithms Used}
\label{sec:algorithms}

\begin{algorithm}
\caption{Reward Calculation}
\begin{algorithmic}[1]
\Require Stopline lane PCU values, Outgoing lane PCU values, Vehicle waiting times
\Ensure Reward value
\Procedure{CalculateReward}{}
    \State $\text{incoming\_pcu} \leftarrow \sum_{l \in L_{\text{stop}}} \text{PCU}(l)$
    \State $\text{outgoing\_pcu} \leftarrow \sum_{l \in L_{\text{out}}} \text{PCU}(l)$
    \State $\text{weighted\_wait} \leftarrow 0$
    \For{all $l \in L_{\text{stop}}$}
        \For{all $v \in l$}
            \State $\text{weighted\_wait} \leftarrow \text{weighted\_wait} + \text{PCU}(v) \cdot w^{acc}(v)$
        \EndFor
    \EndFor
    \State $\text{reward} \leftarrow -(\text{incoming\_pcu} - \text{outgoing\_pcu}) - \lambda \cdot \text{weighted\_wait}$
    \State \Return $\text{reward}$
\EndProcedure
\end{algorithmic}
\end{algorithm}

\begin{algorithm}
\caption{DQN Training Process}
\begin{algorithmic}[1]
\Require Number of episodes $E = 300$, Replay buffer capacity $B = 10000$, 
         Target network update frequency $F = 10$, Batch size $= 64$
\Ensure Trained Q-network
\Procedure{TrainDQN}{$E, B, F$}
    \State Initialize $\epsilon \leftarrow 1.0$, $\epsilon_{min} \leftarrow 0.01$, $\epsilon_{decay} \leftarrow 0.985$
    \State Initialize replay buffer with capacity $B$
    \State Initialize Q-network and target network with random weights
    \For{$episode \leftarrow 1$ to $E$}
        \State Reset environment
        \State $state \leftarrow \text{GetInitialState()}$
        \While{simulation is running}
            \State $action \leftarrow \text{SelectAction}(state, \epsilon)$ \Comment{$\epsilon$-greedy selection}
            \State Execute $action$ for 15 seconds
            \State $reward \leftarrow \text{CalculateReward()}$
            \State $next\_state \leftarrow \text{GetNextState()}$
            \State Store $(state, action, reward, next\_state)$ in replay buffer
            \If{replay buffer has $\geq 64$ samples}
                \State Sample random batch of 64 from replay buffer
                \State Calculate target: $Q_{target} = r + \gamma \times \max_{a'}(Q(s', a'))$
                \State Calculate current: $Q_{current} = Q(s, a)$
                \State Compute loss: $\mathcal{L} = \text{MSE}(Q_{target}, Q_{current})$
                \State Update Q-network weights via backpropagation
            \EndIf
            \State $state \leftarrow next\_state$
        \EndWhile
        \State $\epsilon \leftarrow \max(\epsilon_{min}, \epsilon \times \epsilon_{decay})$ \Comment{Decay exploration rate}
        \If{$episode \mod F = 0$}
            \State Update target network weights from Q-network
        \EndIf
        \If{$episode \mod 5 = 0$}
            \State Save model checkpoint
        \EndIf
    \EndFor
    \State \Return trained Q-network
\EndProcedure
\end{algorithmic}
\end{algorithm}
